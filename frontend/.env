# URL da API (servidor Python). Em desenvolvimento usa http://localhost:8000 se não definir.
VITE_API_URL=http://localhost:8001

# Avatar padrão do perfil (URL da imagem). Deixe em branco ou use uma URL real.
# VITE_PROFILE_AVATAR_URL=